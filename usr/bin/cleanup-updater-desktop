#!/bin/bash
# Remove updater desktop file if it exists
DESKTOP_FILE="$HOME/.local/share/applications/org.bigcommunity.appimage.updater.desktop"

if [ -f "$DESKTOP_FILE" ]; then
    rm "$DESKTOP_FILE"
    echo "Removed $DESKTOP_FILE"
else
    echo "File not found: $DESKTOP_FILE"
fi
